version: '3.8'

services:
  chroma:
    image: chromadb/chroma:0.5.4
    container_name: chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma-data:
